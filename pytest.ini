[pytest]
testpaths = tests
# The add-on package (`app`) lives in familylink-playwright/ and the
# integration in custom_components/; tests/conftest.py puts both on sys.path
# before any test module is imported.
python_files = test_*.py
addopts = --strict-markers
filterwarnings =
    default
